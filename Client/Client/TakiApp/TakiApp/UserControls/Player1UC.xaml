<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TakiApp.Utilities"
             x:Class="TakiApp.UserControls.Player1UC">
    <ContentView.Resources>
        <ResourceDictionary>
            <local:SourceConverter x:Key="SourceConverter" />
        </ResourceDictionary>
    </ContentView.Resources>
    <Grid x:Name="MyGrid" Rotation="270">
        <ListView x:Name="HandView" HasUnevenRows="True" SeparatorVisibility="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Image Margin="0,-20,0,0" Rotation="90" Source="{Binding Image, Converter={StaticResource SourceConverter}}" HeightRequest="135" Aspect="AspectFit">
                        </Image>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentView>