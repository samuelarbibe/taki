<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TakiApp.Utilities"
             x:Class="TakiApp.UserControls.Player1UC">
    <ContentView.Resources>
        <ResourceDictionary>
            <local:SourceConverter x:Key="SourceConverter" />
        </ResourceDictionary>
    </ContentView.Resources>
    <Grid x:Name="MyGrid" Rotation="270">
        <CollectionView x:Name="HandView">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Image WidthRequest="55" Source="{Binding Image, Converter={StaticResource SourceConverter}}" HeightRequest="100" Aspect="AspectFit"></Image>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentView>